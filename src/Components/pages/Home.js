import React, { Fragment } from 'react';
import SearchBar from '../SearchBar';
import Users from '../Users';

const Home = () => (
  <Fragment>
    <SearchBar />
    <Users />
  </Fragment>
);

export default Home;
